name: C/C++ CI (Windows x64)

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Setup MSBuild.exe
      uses: warrenbuckley/Setup-MSBuild@v2
    - name: Compile
      run: msbuild "bin2c.sln" /m /verbosity:minimal
      env:
        Platform: x64
    - name: Tests
      run: >-
        Release/x64/bin2c /custvar "IMAGE_VP" /nohead /infile "./tests/VP.jpg" /outfile "./tests/VP.hpp" /basefolder "./tests/"
        
        msbuild "tests/VP.sln"
